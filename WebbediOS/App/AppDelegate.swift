import UIKit
import WebbedKit

/// Lightweight UIKit lifecycle stub; the app is SwiftUI-driven via `WebbedApp`.
final class WebbedAppDelegate: NSObject, UIApplicationDelegate {
    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey : Any]? = nil) -> Bool {
        true
    }
}
